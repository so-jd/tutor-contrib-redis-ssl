# Update Redis URLs to use configurable transport
CACHES["default"]["LOCATION"] = f"{{ REDIS_TRANSPORT }}://{{ REDIS_HOST }}:{{ REDIS_PORT }}/{{ OPENEDX_CACHE_REDIS_DB }}"
CACHES["general"]["LOCATION"] = f"{{ REDIS_TRANSPORT }}://{{ REDIS_HOST }}:{{ REDIS_PORT }}/{{ OPENEDX_CACHE_REDIS_DB }}"

# Update Celery broker URL
CELERY_BROKER_URL = f"{{ REDIS_TRANSPORT }}://{{ REDIS_HOST }}:{{ REDIS_PORT }}/{{ OPENEDX_CELERY_REDIS_DB }}"